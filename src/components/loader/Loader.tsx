import React from "react";

// Loader - loading progress bar
const Loader = () => (
  <div className="flex justify-center h-screen items-center">
    <progress className="progress w-56 progress-primary"></progress>
  </div>
);

export default Loader;
